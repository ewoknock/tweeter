<%= turbo_stream.replace "follow-button" do %>
    <%= render partial: "users/follow_button", locals: { user: @user.following_users.find(params[:following_user_id]), current_user: current_user } %>
<% end %>